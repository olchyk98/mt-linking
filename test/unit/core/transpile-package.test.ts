import { describe, it } from 'vitest'

describe.concurrent('transpilePackage', () => {
  it('should return null if no package can be resolved at specified path', () => {

  })

  it('should return null if no linking strategy works with the package', () => {

  })

  it('should properly transpile package with linking strategy: TRANSPILED', async () => {

  })

  it('should properly transpile package with linking strategy: TRANSPILED_LEGACY', async () => {

  })

  it('should properly transpile package with linking strategy: AMEND_NATIVE', async () => {

  })

  it('should properly transpile package with linking strategy: NOBUILD_SOURCE', async () => {

  })

  it('should properly transpile package with linking strategy: MAKEFILE_BUILD', async () => {

  })
})
