import { describe, it } from 'vitest'

describe.concurrent('getLinkablePackages', () => {
  it('should return an empty array if specified root path has no items', () => {

  })

  it('should resolve single level hierarchy of links', () => {

  })

  it('should resolve nested level hierarchy of links', () => {

  })
})
