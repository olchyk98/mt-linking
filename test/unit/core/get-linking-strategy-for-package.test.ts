import { describe, it } from 'vitest'

describe.concurrent('getLinkingStrategyForPackage', () => {
  it('should return null if package at specified path can not be resolved', () =>{

  })

  it('should properly identify strategy: TRANSPILED', () => {

  })

  it('should properly identify strategy: TRANSPILED_LEGACY', () => {

  })

  it('should properly identify strategy: AMEND_NATIVE', () => {

  })

  it('should properly identify strategy: MAKEFILE_BUILD', () => {

  })

  it('should properly identify strategy: NOBUILD_SOURCE', () => {

  })
})
