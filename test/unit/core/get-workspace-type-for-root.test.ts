import { describe, it } from 'vitest'

describe.concurrent('getWorkspaceTypeForRoot', () => {
  it('should return null if input path does not lead to a package', () => {

  })

  it('should return null if package is not a workspace root', () => {

  })

  it('should properly match workspace root type: yarn', () => {

  })

  it('should properly match workspace root type: pnpm', () => {

  })
})
