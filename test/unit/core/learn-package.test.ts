import { describe, it } from 'vitest'

describe.concurrent('learnPackage', () => {
  it('should return null if specified absolutePath does not lead to a valid package', () => {

  })

  it('should properly save specified package in links collection', () => {

  })

  it('should properly save specified package from a workspace in links collection', () => {

  })
})
