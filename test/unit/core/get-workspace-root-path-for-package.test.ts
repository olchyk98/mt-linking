import { describe, it } from 'vitest'

describe.concurrent('getWorkspaceRootPathForPackage', () => {

  it('should return null if package does not belong to workspace', () => {

  })

  it('should return null if package is within workspace folder structure, but not referenced by workspace root config', () => {

  })

  it('should return null when input is 10 levels down in depth', () => {

  })

  it('should return input path when input is root', () => {

  })

  it('should return paths when input is 3 levels down in depth', () => {

  })
})
