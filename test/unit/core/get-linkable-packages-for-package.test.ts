import { describe, it } from 'vitest'

describe.concurrent('getLinkablePackagesForPackage', () => {
  it('should return null if package at specified path cannot be resolved', () => {

  })

  it('should return an empty array if there are no linkable packages', () => {

  })

  it('should return linkable packages that are referenced in dest package.json', () => {

  })

  it('should properly extract all linkable packages for all packages in a workspace', () => {

  })
})
