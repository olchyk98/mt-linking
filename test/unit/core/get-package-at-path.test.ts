import { describe, it } from 'vitest'

describe.concurrent('getPackageAtPath', () => {
  it('should return null if no package.json exists at specified path', () => {

  })

  it('should return null if package.json at specified path is not parsable', () => {

  })

  it('should return null if package.json at specified path does not have name property', () => {

  })

  it('should properly load and resolve package spec (path ends with dirname)', () => {

  })

  it('should properly load and resolve package spec (path ends with filename)', () => {

  })
})
